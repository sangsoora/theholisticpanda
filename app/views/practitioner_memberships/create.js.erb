$("#memberships-list").html('<%= j render "practitioner_memberships_list", practitioner: @practitioner %>');
$("#progress").html('<%= j render  "practitioners/profile_progress_bar", practitioner: @practitioner %>');
membershipsForm.style.display = 'none';
membershipsForm.reset();
membershipsEdit.style.display = 'block';
<% if @practitioner_membership.errors.empty? %>
  <% flash[:notice] = "Membership has been added." %>
  $("#flashes").html('<%= j render partial: "shared/flashes", locals: { notice: flash[:notice] } %>');
<% else %>
  $("#flashes").html('<%= j render partial: "shared/flashes", locals: { alert: flash[:alert] } %>');
<% end %>
$(function() {
  setTimeout(function(){
    $('.alert').slideUp(500);
  }, 2000);
});
if (document.querySelector('.tooltip')) {
  document.querySelector('.tooltip').remove();
}
