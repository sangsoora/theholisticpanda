Dear <%= @session.user.first_name %>,

This is a notice to let you know that the following session with <%= @practitioner.user.full_name %> has been cancelled.

Service: <%= @session.service.name %>

Starting time: <%= @session.start_time.in_time_zone(@session.user.timezone).strftime('%a, %b %d, %Y %l:%M %p %Z') %> <%= @session.user.timezone %>

Total Price: CA$ <%= @session.estimate_price.round(2) %>

<% if (@session.cancelled_user != @session.user) && @session.cancel_reason %>
Cancel reason from your practitioner: <%= @session.cancel_reason %>
<% end %>

Yours,
The Holistic Panda Team
